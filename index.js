function addingEventListener() {
    const input = document.getElementById('input');

function clickAlert() {
  alert('wagwan flaminum!');
}

input.addEventListener('click', clickAlert);
}
addingEventListener();